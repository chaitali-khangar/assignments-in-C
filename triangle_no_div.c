# include <stdio.h>
# include <stdbool.h>

int main()
{
	int num,sum,reminder;
	bool divisible;
	sum = 0;

	for (num = 1; num<=11 ; num++){
		divisible = true;
		sum = num + sum;


	for (int i = 1; i < num; i++){
		reminder = sum%i


		
			/* code */
		}
	/* code */
		}
			if (divisible)
				{
					/* code */
				}	/* code */
		}	
	return 0;
}